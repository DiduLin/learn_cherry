# Cherry Framework 学习 & 源码钻研清单（v2）

> **学习哲学：先搭骨架，再填血肉**
> 每个模块：先建立心智模型（骨架题）→ 再理解机制 → 最后深入实现细节
> 完成后将 `- [ ]` 改为 `- [x]`
> 最后更新：2026-09-15

---

## 第 0 题：把框架跑起来（建立体感）

- [X] **0.1** 环境准备：Go 1.24+、Git、NATS、etcd 就绪，`go version` 验证
- [X] **0.2** 运行 `examples/demo_chat`，客户端成功连接
- [X] **0.3** 运行 `examples/demo_cluster`，观察多节点发现与通信
- [X] **0.4** 用 Builder API 写最小服务（1 组件 + 1 Actor），SIGINT 关闭观察逆序停止
- [X] **0.5** IDE 断点跟踪：`main.go` → `cherry.New` → `Start` → `Stop` 完整调用链

> ✅ 验收：5 分钟内从零拉起服务，能解释启动/关闭时发生了什么 
>
> 	启动 cherry 时，通过 cherry.Configurate 方法加载配置文件 profile（可以在 examples 获取），同时设置要启动的 NodeID 和节点模式，创建并返回一个 AppBuilder 对象。通过这个 AppBuilder 对象我们可以注册自己的 Component、Actor 和一些生命周期方法，比如 OnShutdown。
> 	然后调用 AppBuilder.Startup 方法启动服务，服务启动会先注册 ICluster 集结组件 和 IDescovery 注册中心组件（使用 ClusterMode 的情况下），然后加载 ActorSystem，再加载 NetParser 网络消息组件（设置 isFrontend=true 的情况），然后按照 Compoent  组件的注册顺序，依次调用 OnInit -> OnAfterInit 方法，最后主协程监听中断信号并挂起。此时 Actor 协程已运行
> 	当收到 SIGINT 中断信号后，会按照 Component 注册逆序调用 OnBeforeStop -> OnStop，然后调用所有 OnShutdown 方法，最后程序退出。

---

## 模块一：整体架构（骨架先行）

> 📌 **本模块是所有其他模块的前置依赖。** 先建立"Cherry 长什么样"的全局视图。

### 1.1 骨架题：Cherry 的整体架构图

- [ ] **1.1.1** 【骨架】Cherry 由哪些核心子系统组成？画出你自己的架构图（标注：Actor / 组件 / 发现 / RPC / 连接器 / 协议）
- [ ] **1.1.2** 【骨架】一次客户端请求从连接到被 Actor 处理的完整路径是什么？画时序图

### 1.2 设计定位

- [ ] **1.2.1** 为什么说它是"Actor Model + Go"？和传统 goroutine pool / 线程池的本质区别是什么？
- [ ] **1.2.2** Builder API 的设计意图？对比 `cherry.New()` + 手动注册组件的优劣

### 1.3 源码入口

- [ ] **1.3.1** `cherry.New()` 内部做了什么？返回的对象包含哪些字段？
- [ ] **1.3.2** `Start()` 的启动顺序是什么？各子系统按什么顺序初始化？
- [ ] **1.3.3** 框架的分层目录结构（core / actor / net / discovery / rpc）各自的职责边界是什么？

> ✅ 验收：不看文档，能口头描述 Cherry 的整体架构 + 一次请求的生命周期

---

## 模块二：生命周期体系（骨架 → 机制）

> 📌 **前置：模块一**
> 生命周期是理解组件和 Actor 如何协作的基础。

### 2.1 骨架题：生命周期全景

- [ ] **2.1.1** 【骨架】Cherry 中有几种"生命周期"？（组件 / Actor / 连接 / 进程）它们之间的关系是什么？
- [ ] **2.1.2** 【骨架】画一张表：各生命周期的阶段（Init → Start → Run → Stop）及触发时机

### 2.2 组件生命周期

- [ ] **2.2.1** 组件接口定义：必须实现哪些方法？可选钩子有哪些？
- [ ] **2.2.2** "组件按注册的逆序停止"具体怎么实现？源码在哪一段？
- [ ] **2.2.3** 组件启动失败时的回滚逻辑是什么？

### 2.3 进程级生命周期

- [ ] **2.3.1** 优雅关闭完整流程：信号捕获 → 停止组件 → Actor 停止 → 退出进程，每步源码在哪？
- [ ] **2.3.2** `SIGINT` vs `SIGTERM` 处理有区别吗？

### 2.4 源码深入

- [ ] **2.4.1** 生命周期管理器的数据结构是什么？如何保证顺序性？
- [ ] **2.4.2** 生命周期钩子中 panic 了会怎样？框架如何隔离？

> ✅ 验收：能解释"为什么组件要逆序停止"以及"优雅关闭时每个子系统在做什么"

---

## 模块三：Actor 模型（骨架 → 机制 → 实现）

> 📌 **前置：模块一、模块二**
> 这是 Cherry 的核心。分三层递进。

### 3.1 骨架题：Actor 心智模型

- [ ] **3.1.1** 【骨架】Actor 的本质是什么？用一句话 + 一张图描述：一个 Actor = 状态 + 行为 + 邮箱
- [ ] **3.1.2** 【骨架】Actor 之间如何通信？为什么"不要调用方法，要发消息"？
- [ ] **3.1.3** 【骨架】Actor 与 goroutine 的映射关系？一个 Actor 永远在同一个 goroutine 上执行吗？

### 3.2 Actor 基础机制

- [ ] **3.2.1** Actor 接口定义：必须实现哪些方法？
- [ ] **3.2.2** Actor 的创建与注册流程：从 `NewActor()` 到可被调度的完整链路
- [ ] **3.2.3** 三种独立队列（mailbox / timer / ?）的 FIFO 消费逻辑在哪里实现？
- [ ] **3.2.4** 同步消息 vs 异步消息：语义区别 + 代码层面的实现差异
- [ ] **3.2.5** Actor 之间发消息的 API 有哪些？底层都走了什么路径？

### 3.3 子 Actor 与层级

- [ ] **3.3.1** 父 Actor 如何创建子 Actor？API 是什么？
- [ ] **3.3.2** 消息为什么由父 Actor 路由转发？好处是什么？
- [ ] **3.3.3** 父 Actor 停止时，子 Actor 如何被级联停止？源码中的实现逻辑？
- [ ] **3.3.4** 子 Actor panic 会传播到父 Actor 吗？

### 3.4 Actor Timer

- [ ] **3.4.1** Actor Timer 与普通 `time.AfterFunc` 的区别？
- [ ] **3.4.2** 定时任务如何保证在 Actor 自己的 goroutine 上执行？
- [ ] **3.4.3** Timer 在 Actor 重启/停止时的行为是什么？资源如何回收？

### 3.5 深入实现（进阶）

- [ ] **3.5.1** mailbox 的底层数据结构是什么？为什么选它？
- [ ] **3.5.2** mailbox 满时的处理策略：丢弃 / 阻塞 / 扩容？
- [ ] **3.5.3** Actor panic 后的恢复机制：recover 在哪里？是否影响其他 Actor？
- [ ] **3.5.4** 是否存在"Actor 饥饿"问题？框架如何缓解？
- [ ] **3.5.5** 多节点下，同一个 Actor 是否可能同时存在多个实例？如何避免？
- [ ] **3.5.6** Actor 与节点绑定的策略在哪里实现？
- [ ] **3.5.7** 网络分区时，Actor 层面的表现是什么？

### 3.6 扩展设计（完全掌握）

- [ ] **3.6.1** 如果要为 Actor 增加"优先级消息"能力，需要改哪些核心结构？
- [ ] **3.6.2** 当前 mailbox 是否存在锁竞争？如何用无锁结构优化？
- [ ] **3.6.3** 在 10w+ Actor 场景下，goroutine 数量如何控制？
- [ ] **3.6.4** 如果要支持 Actor 迁移（从一个节点移到另一个节点），需要哪些改动？

> ✅ 验收：能独立设计一个基于 Actor 的业务模块，并解释为什么这样设计

---

## 模块四：服务发现（骨架 → 机制 → 实现）

> 📌 **前置：模块一**
> 分布式系统的"地图"。

### 4.1 骨架题：发现机制全景

- [ ] **4.1.1** 【骨架】服务发现在 Cherry 中解决什么问题？没有它行不行？
- [ ] **4.1.2** 【骨架】三种发现后端（NATS / etcd / ?）各自的特点和适用场景？

### 4.2 机制与流程

- [ ] **4.2.1** 节点上线时：注册流程是什么？其他节点如何被通知？
- [ ] **4.2.2** 节点下线时：主动注销 vs 心跳超时，分别怎么处理？
- [ ] **4.2.3** 通过什么 profile 配置切换发现后端？
- [ ] **4.2.4** NATS / etcd 在 Cherry 中扮演什么角色？是强依赖还是可插拔？

### 4.3 深入实现

- [ ] **4.3.1** 发现模块的接口定义是什么？新增后端需要实现哪些方法？
- [ ] **4.3.2** 发现模块与 RPC 模块如何解耦？通过什么机制联动？
- [ ] **4.3.3** 网络分区时，发现模块的表现是什么？脑裂如何避免？

### 4.4 扩展设计

- [ ] **4.4.1** 如果要新增 Consul 作为发现后端，需要改哪些接口？
- [ ] **4.4.2** 如果 NATS 宕机，系统降级策略是什么？

> ✅ 验收：能解释"节点 A 如何知道节点 B 的存在与消失"

---

## 模块五：RPC 体系（骨架 → 机制 → 实现）

> 📌 **前置：模块一、模块四**
> 跨节点通信的核心。

### 5.1 骨架题：RPC 全景

- [ ] **5.1.1** 【骨架】Cherry 的 RPC 调用链路是什么？客户端 → 本地 Actor → 远程 Actor → 返回，画时序图
- [ ] **5.1.2** 【骨架】同步 RPC vs 异步 RPC 的语义区别？各自适用什么场景？

### 5.2 机制与流程

- [ ] **5.2.1** 基于 NATS 的 RPC 请求/响应如何关联？（correlation id？reply subject？）
- [ ] **5.2.2** 同步 RPC 在代码层面如何实现"等待响应"？channel？future？
- [ ] **5.2.3** 异步 RPC 的回调如何被调度回目标 Actor 的 goroutine？
- [ ] **5.2.4** 超时机制如何实现？超时后资源（channel / goroutine）如何回收？
- [ ] **5.2.5** RPC 失败重试策略在哪里配置？默认行为是什么？

### 5.3 序列化与协议

- [ ] **5.3.1** RPC 请求/响应的序列化方式是什么？是否可插拔？
- [ ] **5.3.2** 消息编解码的性能瓶颈在哪里？对象池如何介入？

### 5.4 深入实现

- [ ] **5.4.1** RPC client / server 的初始化流程
- [ ] **5.4.2** RPC 调用过程中的错误处理：网络错误 / 目标 Actor 不存在 / 超时 / 序列化失败
- [ ] **5.4.3** 高并发 RPC 下的性能瓶颈点在哪里？

### 5.5 扩展设计

- [ ] **5.5.1** 如果要替换 NATS 为 Kafka / RabbitMQ，影响范围有多大？
- [ ] **5.5.2** 如果要支持 RPC 流式调用（streaming），需要改哪些层？

> ✅ 验收：能完整描述一次跨节点 RPC 调用的每一步，包括失败场景

---

## 模块六：连接器与协议（骨架 → 机制 → 实现）

> 📌 **前置：模块一**
> 客户端接入的入口。

### 6.1 骨架题：连接模型

- [ ] **6.1.1** 【骨架】连接器在 Cherry 中的角色是什么？它和 Actor 的关系是什么？
- [ ] **6.1.2** 【骨架】客户端连接的生命周期：建立 → 认证 → 业务通信 → 断线 → 重连

### 6.2 连接器

- [ ] **6.2.1** 内置连接器有哪些？（TCP / WebSocket / ?）各自适用什么场景？
- [ ] **6.2.2** 连接器的接口定义：必须实现哪些方法？
- [ ] **6.2.3** 高并发连接下，连接器如何避免成为瓶颈？
- [ ] **6.2.4** 粘包/拆包在哪里处理？具体算法是什么？

### 6.3 协议

- [ ] **6.3.1** 两种协议格式分别是什么？各自的包头结构？
- [ ] **6.3.2** 协议编解码的实现位置？性能关键点？
- [ ] **6.3.3** "兼容 pomelo 协议"具体兼容到什么程度？客户端接入需要注意什么？

### 6.4 深入实现

- [ ] **6.4.1** 连接建立后，如何关联到具体的 Actor？
- [ ] **6.4.2** 断线检测机制：心跳间隔、超时策略
- [ ] **6.4.3** 连接级别的限流/熔断在哪里实现？

> ✅ 验收：能解释"客户端发一条消息到被 Actor 处理"的完整路径

---

## 模块七：性能基础设施（工具 → 原理）

> 📌 **前置：模块一**
> 这些是所有上层模块的性能底座。

### 7.1 骨架题：性能设计理念

- [ ] **7.1.1** 【骨架】Cherry 的性能设计核心原则是什么？（零分配？对象复用？减少锁？）
- [ ] **7.1.2** 【骨架】哪些地方用了对象池？解决了什么问题？

### 7.2 对象池

- [ ] **7.2.1** "基于对象池的零分配消息传递"如何实现？（sync.Pool？自定义池？）
- [ ] **7.2.2** 对象池命中率低时如何排查？
- [ ] **7.2.3** 对象池中的对象何时归还？归还时的清理逻辑？

### 7.3 工具包

- [ ] **7.3.1** 框架内置的 21 个工具包分别是什么？各解决什么问题？
- [ ] **7.3.2** 挑 5 个高频使用的深入看：接口设计 + 使用场景 + 实现原理

### 7.4 错误码与日志

- [ ] **7.4.1** 分层错误码的设计意图？如何自定义业务错误码？
- [ ] **7.4.2** `uber-go/zap` 封装点在哪里？日志字段规范是什么？
- [ ] **7.4.3** 如何通过日志还原一次跨节点 RPC 调用？

> ✅ 验收：能解释"为什么 Cherry 能在高并发下保持低延迟"

---

## 模块八：组件生态（应用层）

> 📌 **前置：模块二、模块三**
> 框架之外的"插件世界"。

### 8.1 骨架题：组件生态全景

- [ ] **8.1.1** 【骨架】`components` 仓库中有哪些组件？各自解决什么问题？
- [ ] **8.1.2** 【骨架】第三方组件如何与 Actor 体系集成？

### 8.2 核心组件

- [ ] **8.2.1** DB 写队列组件：实现原理？为什么用队列解耦？
- [ ] **8.2.2** `gopher-lua` 脚本组件：Lua VM 与 Actor 的关系？生命周期如何绑定？
- [ ] **8.2.3** 限流组件：基于什么算法？作用在哪个层级（连接 / Actor / RPC）？

### 8.3 自定义组件

- [ ] **8.3.1** 如何开发一个自定义组件？最小接口是什么？
- [ ] **8.3.2** 组件如何访问 Actor 系统？如何注册到生命周期？

> ✅ 验收：能独立开发一个自定义组件并集成到服务中

---

## 模块九：可观测性与稳定性（运维视角）

> 📌 **前置：全部模块的基础理解**

### 9.1 骨架题：可观测性全景

- [ ] **9.1.1** 【骨架】当前框架提供了哪些可观测能力？（日志 / metrics / tracing / pprof）
- [ ] **9.1.2** 【骨架】线上问题定位的"标准流程"是什么？

### 9.2 实现

- [ ] **9.2.1** 框架提供了哪些 metrics？如何接入 Prometheus？
- [ ] **9.2.2** 有没有现成的 pprof / debug 接口？
- [ ] **9.2.3** 如何模拟网络分区、NATS 宕机等异常场景做测试？
- [ ] **9.2.4** 混沌工程在 Cherry 项目中如何落地？

> ✅ 验收：能独立分析一个线上问题（Actor 卡死 / RPC 超时 / 内存上涨）并定位到具体模块

---

## 模块十：参与框架开发（贡献者视角）

> 📌 **前置：全部模块**

### 10.1 工程规范

- [ ] **10.1.1** 项目目录结构规范？新增组件放哪里？
- [ ] **10.1.2** Issue / PR 模板要求？
- [ ] **10.1.3** Commit message 规范（Conventional Commits？）
- [ ] **10.1.4** 如何运行完整 CI（lint / test / build）？

### 10.2 贡献路径

- [ ] **10.2.1** 从 Good First Issue 到核心模块的演进路径
- [ ] **10.2.2** 如何补充/改进文档？（尤其是中文文档）
- [ ] **10.2.3** 如何为 `examples/demo_*` 增加新示例
- [ ] **10.2.4** 成功提交并被合并至少一个 PR

### 10.3 长期演进

- [ ] **10.3.1** 当前框架的主要技术债？（从 issue / discussion 中总结）
- [ ] **10.3.2** 下一个大版本可能的演进方向？
- [ ] **10.3.3** 如果你来设计 Cherry v2，会在哪些地方做 breaking change？

> ✅ 验收：提交并被合并至少一个 PR（文档 / bugfix / feature）

---

## 学习策略

1. **模块间有依赖**：先完成前置模块，再进入后续模块
2. **模块内按编号顺序**：骨架题 → 机制题 → 实现题 → 扩展题
3. **每个问题输出答案卡片**：
   - 一句话结论
   - 关键源码位置（文件:行号）
   - 调用链时序图
   - 自己的疑问
4. **边学边改**：每学完一个模块，做一个小实验（改日志 / 加 RPC / 加指标）

---

## 依赖关系速查

第 0 题（跑起来）
↓
模块一：整体架构 ← 所有模块的前置
↓
模块二：生命周期 ← 模块三、模块八的前置
模块四：服务发现 ← 模块五的前置
模块五：RPC ← 模块四
模块六：连接器与协议
模块七：性能基础设施
↓
模块八：组件生态（需模块二、三）
模块九：可观测性（需全部基础模块）
模块十：参与开发（需全部模块）

---

> 📌 进度追踪：每完成一题，把 `- [ ]` 改为 `- [x]`
> 📌 建议配合 `git checkout -b study/xxx` 分支进行源码标注和实验
> 📌 文件编码：UTF-8
