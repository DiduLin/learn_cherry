# Cherry Framework 学习 & 源码钻研清单

> 目标：从入门 → 进阶 → 完全掌握 → 参与框架开发
> 方法：带着问题啃源码，每题完成后打勾 ✅
> 最后更新：2026-09-15

---

## 第 0 题（必做）：把框架跑起来

- [X] **0.1 环境准备**
  - Go 1.24+ 是否已安装？`go version` 验证。
  - Git、NATS、etcd（按需）是否就绪？
- [X] **0.2 运行官方示例**
  - `examples/demo_chat`：本地直接跑起来，客户端能否连上？
  - `examples/demo_cluster`：多节点启动，观察节点发现与通信。
- [ ] **0.3 最小自定义服务**
  - 用 Builder API 写一个**只含 1 个组件 + 1 个 Actor** 的最小服务。
  - 通过 `SIGINT/SIGTERM` 关闭，观察**逆序停止**日志。
- [ ] **0.4 调试入口**
  - 找到 `main.go` → `cherry.New` → `Start` 的调用链。
  - 在 IDE 中给 `Start()`、`Stop()`、`OnInit()`、`OnStop()` 打断点，单步跟踪。

✅ **验收标准**：你能 5 分钟内从零拉起一个 Cherry 服务，并解释启动/关闭时发生了什么。

---

## 第一阶段：入门（建立整体心智模型）

### 1. 架构与定位

- [ ] 1. Cherry 的整体架构图是怎样的？（画一张自己的）
- [ ] 2. 为什么说它是“Actor Model + Go”？和传统 goroutine pool 有什么区别？
- [ ] 3. Builder API 的设计意图是什么？对比 `cherry.New()` + 手动注册组件。

### 2. 生命周期

- [ ] 4. 组件生命周期钩子有哪些？（Init / Start / Stop 等）
- [ ] 5. “组件按注册的逆序停止”具体是怎么实现的？源码在哪一段？
- [ ] 6. 优雅关闭的完整流程：信号捕获 → 停止组件 → Actor 停止 → 退出进程。

### 3. Actor 基础

- [ ] 7. Actor 的接口定义是什么？必须实现哪些方法？
- [ ] 8. 一个 Actor 是如何被调度到**独立 goroutine** 的？
- [ ] 9. 三种独立队列（mailbox / timer / ？）的 FIFO 消费逻辑在哪里实现？
- [ ] 10. Actor 之间如何发消息？同步/异步语义分别是什么？

### 4. 子 Actor 与生命周期

- [ ] 11. 父 Actor 如何创建子 Actor？
- [ ] 12. 消息为什么由父 Actor 路由转发？好处是什么？
- [ ] 13. 父 Actor 停止时，子 Actor 如何被级联停止？

### 5. Actor Timer

- [ ] 14. Actor Timer 与普通 `time.AfterFunc` 的区别？
- [ ] 15. 定时任务如何保证在 Actor 自己的 goroutine 上执行？
- [ ] 16. Timer 在 Actor 重启/停止时的行为是什么？

### 6. 服务发现

- [ ] 17. 三种发现后端分别是什么？通过什么 profile 配置切换？
- [ ] 18. NATS / etccd 在 Cherry 中扮演什么角色？
- [ ] 19. 节点上下线时，发现模块如何通知其他模块？

### 7. RPC

- [ ] 20. 基于 NATS 的跨节点 RPC 调用链路是怎样的？
- [ ] 21. 同步/异步 RPC 在代码层面如何区分？
- [ ] 22. 超时机制是如何实现的？超时后资源如何回收？

### 8. 连接器与协议

- [ ] 23. 内置连接器有哪些？各自适用什么场景？
- [ ] 24. 两种协议格式分别是什么？如何编解码？
- [ ] 25. “兼容 pomelo 协议”具体兼容到什么程度？客户端接入需要注意什么？

### 9. 性能与工具

- [ ] 26. “基于对象池的零分配消息传递”是如何实现的？（sync.Pool？）
- [ ] 27. 框架内置的 21 个工具包分别是什么？挑 5 个高频使用的深入看。
- [ ] 28. 分层错误码的设计意图？如何自定义业务错误码？
- [ ] 29. `uber-go/zap` 封装点在哪里？日志字段规范是什么？

✅ **验收标准**：你能不看文档，口头讲清楚 Cherry 的整体架构和一次请求的生命周期。

---

## 第二阶段：进阶（深入关键子系统）

### 1. Actor 调度与队列

- [ ] 30. Actor mailbox 的底层数据结构是什么？为什么选它？
- [ ] 31. 当 mailbox 满时，框架如何处理？（丢弃 / 阻塞 / 扩容）
- [ ] 32. Actor panic 后的恢复机制在哪里？是否会影响其他 Actor？
- [ ] 33. 是否存在“Actor 饥饿”问题？框架如何缓解？

### 2. 分布式与一致性

- [ ] 34. 多节点下，同一个 Actor 是否可能同时存在多个实例？如何避免？
- [ ] 35. Actor 与节点绑定的策略在哪里实现？
- [ ] 36. 网络分区时，发现模块和 RPC 模块分别如何表现？

### 3. RPC 深度

- [ ] 37. RPC 请求/响应的序列化方式是什么？是否可插拔？
- [ ] 38. 异步 RPC 的回调是如何被调度回目标 Actor 的？
- [ ] 39. RPC 失败重试策略在哪里配置？默认行为是什么？

### 4. 连接器与网络

- [ ] 40. TCP/WebSocket 连接的生命周期管理（建立 / 认证 / 断线 / 重连）。
- [ ] 41. 粘包/拆包在哪里处理？
- [ ] 42. 高并发连接下，连接器如何避免成为瓶颈？

### 5. 组件生态

- [ ] 43. `components` 仓库中 DB 写队列的实现原理？
- [ ] 44. `gopher-lua` 脚本组件：Lua VM 与 Actor 的关系？
- [ ] 45. 限流组件：基于什么算法？作用在哪个层级（连接 / Actor / RPC）？

### 6. 可观测性

- [ ] 46. 框架目前提供了哪些 metrics / tracing 能力？
- [ ] 47. 如何通过日志还原一次跨节点 RPC 调用？
- [ ] 48. 有没有现成的 pprof / debug 接口？

✅ **验收标准**：你能独立分析一个线上问题（如 Actor 卡死、RPC 超时、内存上涨）并定位到具体模块。

---

## 第三阶段：完全掌握（能改、能扩、能优化）

### 1. 扩展性设计

- [ ] 49. 如果要新增一种服务发现后端（如 Consul），需要改哪些接口？
- [ ] 50. 如果要替换 NATS 为 Kafka/RabbitMQ，影响范围有多大？
- [ ] 51. 如何为 Actor 增加“优先级消息”能力？需要改哪些核心结构？

### 2. 性能优化

- [ ] 52. 当前 mailbox 实现是否存在锁竞争？如何用无锁结构优化？
- [ ] 53. 对象池命中率低时，如何排查？
- [ ] 54. 在 10w+ Actor 场景下，goroutine 数量如何控制？

### 3. 稳定性与测试

- [ ] 55. 框架自身的单元测试 / 集成测试覆盖策略？
- [ ] 56. 如何模拟网络分区、NATS 宕机等异常场景？
- [ ] 57. 混沌工程在 Cherry 项目中如何落地？

### 4. 源码组织

- [ ] 58. 为什么代码要这样分层？（core / actor / net / discovery / rpc …）
- [ ] 59. 哪些包是“稳定 API”，哪些是“内部实现，随时可改”？
- [ ] 60. Go 1.24+ 的新特性在项目中是如何被利用的？

✅ **验收标准**：你能对一个“中等难度”的框架级需求给出**设计方案 + 影响评估 + 测试用例**。

---

## 第四阶段：参与框架开发（从贡献者到维护者）

### 1. 工程与协作

- [ ] 61. 项目目录结构规范是什么？新增组件应放在哪里？
- [ ] 62. Issue / PR 模板有哪些要求？
- [ ] 63. Commit message 规范（是否遵循 Conventional Commits）？
- [ ] 64. 如何运行完整的 CI 检查（lint / test / build）？

### 2. 贡献路径

- [ ] 65. 从 Good First Issue 到核心模块的演进路径是什么？
- [ ] 66. 如何为框架补充/改进文档？（尤其是中文文档）
- [ ] 67. 如何为 `examples/demo_*` 增加新示例？

### 3. 长期演进

- [ ] 68. 当前框架的主要技术债在哪里？（从 issue / discussion 中总结）
- [ ] 69. 下一个大版本可能的演进方向是什么？
- [ ] 70. 如果你来设计 Cherry v2，会在哪些地方做 breaking change？

✅ **验收标准**：你成功提交并被合并至少一个 PR（文档 / bugfix / feature 均可）。

---

## 学习策略建议

1. **每个问题输出一份“答案卡片”**
   - 一句话结论
   - 关键源码位置（文件:行号）
   - 调用链时序图（mermaid / 手绘）
   - 自己的疑问与思考
2. **边学边改**
   - 每学完一个阶段，就尝试：
     - 改一点日志格式
     - 加一个调试用 RPC
     - 给 Actor 加一个统计指标
3. **尽早参与社区**
   - 哪怕只是回复 Issue、补充文档，也是在建立“框架开发者身份”。

---

> 📌 进度追踪：每完成一题，把 `- [ ]` 改为 `- [x]`
> 📌 建议配合 `git checkout -b study/xxx` 分支进行源码标注和实验
